<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- SEO Meta Tags -->
    <meta name="description" content="See how your tattoo will age over time with AI prediction. Upload your tattoo photo and visualize how it might look in 5, 10, or 20 years.">
    <meta name="keywords" content="tattoo aging, tattoo fading, aged tattoo, tattoo over time, tattoo prediction, tattoo future, tattoo visualization, AI tattoo aging">
    <meta name="author" content="InkOverTime">
    <meta name="robots" content="index, follow">
    
    <!-- Open Graph / Social Media Meta Tags -->
    <meta property="og:title" content="Ink Over Time - See How Your Tattoo Will Age">
    <meta property="og:description" content="Upload your tattoo photo and see how it might look after 5, 10, or 20 years with our AI prediction tool.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= canonicalUrl %>">
    <meta property="og:image" content="<%= canonicalUrl %>img/og-image.jpg">
    
    <link rel="canonical" href="<%= canonicalUrl %>">
    <title>Ink Over Time - See How Your Tattoo Will Age</title>
    <link rel="manifest" href="/manifest.json">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.4.0/css/all.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.4.0/css/all.css"></noscript>
    <script src="https://js.stripe.com/v3/"></script>
    <script>
        const stripePublicKey = "<%= stripePublicKey %>";
        const paymentAmount = "<%= paymentAmount %>";
        
        // Check if we're using a live key or test key
        const isLiveStripe = stripePublicKey && stripePublicKey.startsWith('pk_live');
        console.log(`Using Stripe in ${isLiveStripe ? 'LIVE' : 'TEST'} mode`);
        
        // Debug element to track Stripe environment in the DOM
        document.addEventListener('DOMContentLoaded', function() {
            const debugElement = document.createElement('div');
            debugElement.id = 'stripe-mode-indicator';
            debugElement.style.display = 'none';
            debugElement.dataset.mode = isLiveStripe ? 'live' : 'test';
            document.body.appendChild(debugElement);
        });
    </script>
    
    <!-- Structured Data for SEO -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": "Ink Over Time",
      "url": "<%= canonicalUrl %>",
      "description": "See how your tattoo will age over time with AI prediction. Upload your tattoo photo and visualize how it might look in 5, 10, or 20 years.",
      "applicationCategory": "DesignApplication",
      "operatingSystem": "All",
      "offers": {
        "@type": "Offer",
        "price": "<%= paymentAmount %>",
        "priceCurrency": "GBP"
      }
    }
    </script>
</head>
<body>
    <div class="container">
        <header>
            <h1><i class="fas fa-hourglass-half" aria-hidden="true"></i> InkOverTime</h1>
            <p>See how your tattoo will look over time</p>
        </header>

        <main>
            <% if (typeof apiKeyConfigured !== 'undefined' && !apiKeyConfigured) { %>
                <div class="api-warning">
                    <i class="fas fa-exclamation-triangle"></i>
                    <p>OpenAI API key not configured! Please set your OpenAI API key in the .env file before using this application.</p>
                </div>
            <% } %>
            
            <% if (typeof demoMode !== 'undefined' && demoMode) { %>
                <div class="demo-warning">
                    <i class="fas fa-info-circle"></i>
                    <p>Demo Mode: This app is running in demo mode due to API credit limitations. 
                    Uploads will work, but the aging results will use demonstration images instead of AI-generated ones.</p>
                </div>
            <% } %>
            
            <section class="upload-section">
                <div class="pricing-info">
                    <p class="price-tag"><i class="fas fa-tag"></i> £<%= paymentAmount %> per analysis</p>
                    <p class="price-info">See how your tattoo will age using advanced AI</p>
                </div>

                <form id="tattooForm" enctype="multipart/form-data">
                    <div class="file-input-container">
                        <div class="dropzone" id="dropzone">
                            <i class="fas fa-upload"></i>
                            <p>Drag & drop a tattoo image here or click to browse</p>
                            <input type="file" id="tattooImage" name="tattooImage" accept="image/jpeg, image/png, image/webp" required>
                        </div>
                        <div id="preview-container" class="hidden">
                            <img id="imagePreview" src="#" alt="Preview">
                            <button type="button" id="removeImage"><i class="fas fa-times"></i></button>
                        </div>
                    </div>

                    <div class="time-selector">
                        <label for="timeframe">How far into the future?</label>
                        <select id="timeframe" name="timeframe" required>
                            <option value="" disabled selected>Select timeframe</option>
                            <option value="1 year">1 Year</option>
                            <option value="5 years">5 Years</option>
                            <option value="10 years">10 Years</option>
                            <option value="20 years">20 Years</option>
                            <option value="30 years">30 Years</option>
                        </select>
                    </div>

                    <button type="submit" id="processButton" disabled>
                        <i class="fas fa-credit-card"></i> Pay £<%= paymentAmount %> and See How It Ages
                    </button>
                </form>
            </section>
            
            <!-- Payment Modal -->
            <div id="paymentModal" class="modal hidden">
                <div class="modal-content">
                    <span class="close">&times;</span>
                    <h2>Payment</h2>
                    <p class="payment-amount">Amount: £<%= paymentAmount %></p>
                    <div id="payment-element">
                        <!-- Stripe Elements will be inserted here -->
                    </div>
                    <div id="payment-message" class="payment-message hidden"></div>
                    <button id="submit-payment" class="payment-button">
                        <div class="spinner hidden" id="spinner"></div>
                        <span id="button-text">Pay Now</span>
                    </button>
                </div>
            </div>

            <section id="results" class="results-section hidden">
                <h2>Results</h2>
                <div class="results-container">
                    <div class="result-card">
                        <h3>Original</h3>
                        <div class="image-container">
                            <img id="originalImage" src="#" alt="Original Tattoo">
                        </div>
                    </div>
                    <div class="result-card">
                        <h3>After <span id="timeframeResult"></span></h3>
                        <div class="image-container">
                            <img id="processedImage" src="#" alt="Aged Tattoo">
                        </div>
                    </div>
                </div>
                <button id="newImageButton" class="secondary-button">
                    <i class="fas fa-redo"></i> Process Another Image
                </button>
            </section>

            <div id="loadingOverlay" class="loading-overlay hidden">
                <div class="loading-content">
                    <div class="spinner"></div>
                    <p>Creating your aged tattoo preview...</p>
                    <p class="small">This may take up to 30 seconds</p>
                </div>
            </div>
        </main>

        <footer>
            <p>&copy; 2023 InkOverTime | <a href="#">Privacy Policy</a> | <a href="#">Terms of Service</a></p>
            <p class="disclaimer">Results are AI-generated and for informational purposes only. Actual tattoo aging may differ.</p>
        </footer>
    </div>

    <script src="/js/app.js"></script>
</body>
</html> 